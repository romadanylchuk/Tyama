# jest-expo's @react-native/jest-preset declares a peer on a newer React than
# Expo SDK 56 pins (react must equal react-dom exactly — see package.json).
# The dev-only peer conflict is accepted; this keeps local installs and CI's
# `npm ci` consistent with the committed lockfile.
legacy-peer-deps=true
